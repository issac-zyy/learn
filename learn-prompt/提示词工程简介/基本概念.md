# 基本概念

可以通过简单的prompt给llm提示，获得大量结果，但是结果的质量和输入的prompt的质量与完善度有关

一个prompt可以包含传递到模型的指令或者问题等信息，也可以包含其他详细信息，比如上下文，输入，示例

设计有效的提示词以指导模型执行期望任务的方法被称为**提示工程**。

现阶段的大语言模型能够发挥的功能作用。它们可以用于执行各种高级任务，如文本概括、数学推理、代码生成等。

